 <Window x:Class="CardCreator.MainWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="clr-namespace:CardCreator"
 Title="Card Creator" Height="820" Width="1320"
 Background="#FFF9F9FB" KeyDown="Window_KeyDown">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:GridSizeToBoolConverter x:Key="GridSizeToBool"/>
        <local:EnumToBoolConverter x:Key="EnumToBool"/>
    </Window.Resources>
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Save Template..." Command="{Binding SaveCommand}">
                    <MenuItem.Icon>
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE74E;" FontSize="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Load Template..." Command="{Binding LoadCommand}">
                    <MenuItem.Icon>
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8E5;" FontSize="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Export XAML..." Visibility="Collapsed" Command="{Binding ExportXamlCommand}">
                    <MenuItem.Icon>
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xEDE1;" FontSize="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="Save _Data CSV..." Command="{Binding SaveCsvCommand}"/>
                <MenuItem Header="Load _Data CSV..." Command="{Binding LoadCsvCommand}"/>
                <Separator/>
                <MenuItem Header="Save Card _Images..." Command="{Binding SaveImagesCommand}"/>
                <MenuItem Header="Save Card _Sheets..." Command="{Binding SaveSheetsCommand}"/>
            </MenuItem>
            <MenuItem Header="_Insert">
                <MenuItem Header="Add _Text" Command="{Binding AddTextCommand}">
                    <MenuItem.Icon>
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8D2;" FontSize="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Add _Image" Command="{Binding AddImageCommand}">
                    <MenuItem.Icon>
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8B9;" FontSize="16"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="_Align">
                    <MenuItem Header="Align _Left" Command="{Binding AlignLeftCommand}"/>
                    <MenuItem Header="Align _Center" Command="{Binding AlignCenterCommand}"/>
                    <MenuItem Header="Align _Right" Command="{Binding AlignRightCommand}"/>
                    <Separator/>
                    <MenuItem Header="Align _Top" Command="{Binding AlignTopCommand}"/>
                    <MenuItem Header="Align _Middle" Command="{Binding AlignMiddleCommand}"/>
                    <MenuItem Header="Align _Bottom" Command="{Binding AlignBottomCommand}"/>
                </MenuItem>
                <Separator/>
                <MenuItem Header="Distribute _Horizontally" Command="{Binding DistributeHCommand}"/>
                <MenuItem Header="Distribute _Vertically" Command="{Binding DistributeVCommand}"/>
                <Separator/>
                <MenuItem Header="_Bring Forward" Command="{Binding BringForwardCommand}"/>
                <MenuItem Header="_Send Backward" Command="{Binding SendBackwardCommand}"/>
                <Separator/>
                <MenuItem Header="Card &amp; Sheet Settings..." Command="{Binding SettingsCommand}"/>
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="_Snap" IsCheckable="True" IsChecked="{Binding SnapEnabled}"/>
                <MenuItem Header="_Snap Grid Size">
                    <MenuItem Header="1" IsCheckable="True" IsChecked="{Binding GridSize, Converter={StaticResource GridSizeToBool}, ConverterParameter=1}"/>
                    <MenuItem Header="5" IsCheckable="True" IsChecked="{Binding GridSize, Converter={StaticResource GridSizeToBool}, ConverterParameter=5}"/>
                    <MenuItem Header="10" IsCheckable="True" IsChecked="{Binding GridSize, Converter={StaticResource GridSizeToBool}, ConverterParameter=10}"/>
                    <MenuItem Header="20" IsCheckable="True" IsChecked="{Binding GridSize, Converter={StaticResource GridSizeToBool}, ConverterParameter=20}"/>
                    <MenuItem Header="25" IsCheckable="True" IsChecked="{Binding GridSize, Converter={StaticResource GridSizeToBool}, ConverterParameter=25}"/>
                    </MenuItem>
                <MenuItem Header="_Units">
                    <MenuItem Header="_Inches" IsCheckable="True" IsChecked="{Binding Units, Converter={StaticResource EnumToBool}, ConverterParameter=Inches}"/>
                    <MenuItem Header="_Millimeters" IsCheckable="True" IsChecked="{Binding Units, Converter={StaticResource EnumToBool}, ConverterParameter=Millimeters}"/>
                    <MenuItem Header="_Device Independent" IsCheckable="True" IsChecked="{Binding Units, Converter={StaticResource EnumToBool}, ConverterParameter=DeviceIndependent}"/>
                </MenuItem>
                <MenuItem Header="Show _Guidelines" IsCheckable="True" Visibility="Collapsed" IsChecked="{Binding GuidelinesEnabled}"/>
            </MenuItem>
        </Menu>
        <ToolBar DockPanel.Dock="Top" DataContext="{Binding Inspector}" IsEnabled="{Binding IsText}">
            <ComboBox x:Name="FontFamilyCombo" Width="170" ItemsSource="{Binding FontFamilies}" SelectionChanged="FontFamilyCombo_SelectionChanged">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Source}" FontFamily="{Binding}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <ComboBox x:Name="FontSizeCombo" Width="60" IsEditable="True" ItemsSource="{Binding FontSizes}" SelectionChanged="FontSizeCombo_SelectionChanged" LostFocus="FontSizeCombo_LostFocus" KeyDown="FontSizeCombo_KeyDown"/>
            <Separator/>
            <Button Command="EditingCommands.ToggleBold" CommandTarget="{Binding Element}" FontWeight="Bold" Content="B" Width="20" ToolTip="Bold"/>
            <Button Command="EditingCommands.ToggleItalic" CommandTarget="{Binding Element}" FontStyle="Italic" Content="I" Width="20" ToolTip="Italic"/>
            <Button Command="EditingCommands.ToggleUnderline" CommandTarget="{Binding Element}" Width="20" ToolTip="Underline">
                <TextBlock Text="U" TextDecorations="Underline"/>
            </Button>
            <Button Click="ToggleStrikethrough_Click" Width="24" ToolTip="Strikethrough">
                <TextBlock Text="abc" TextDecorations="Strikethrough"/>
            </Button>
            <Separator/>
            <Button Click="PickColor_Click" ToolTip="Text Color">
                <Border x:Name="ForegroundPreview" Width="16" Height="16" Background="Black" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="A" />
                </Border>
            </Button>
            <Button Click="PickBackgroundColor_Click" ToolTip="Background Color">
                <Border x:Name="BackgroundPreview" Width="16" Height="16" Background="Transparent" BorderBrush="Black" BorderThickness="1"/>
            </Button>
            <Separator/>
            <Button Command="EditingCommands.AlignLeft" CommandTarget="{Binding Element}" ToolTip="Align Left">
                <Image Source="/Icons/AlignLeft.png" Width="18" Height="18" />
            </Button>
            <Button Command="EditingCommands.AlignCenter" CommandTarget="{Binding Element}" ToolTip="Align Center">
                <Image Source="/Icons/AlignCenter.png" Width="18" Height="18" />
            </Button>
            <Button Command="EditingCommands.AlignRight" CommandTarget="{Binding Element}" ToolTip="Align Right">
                <Image Source="/Icons/AlignRight.png" Width="18" Height="18" />
            </Button>
            <Button Command="EditingCommands.AlignJustify" CommandTarget="{Binding Element}" ToolTip="Align Justify">
                <Image Source="/Icons/AlignJustify.png" Width="18" Height="18" />
            </Button>
            <Separator/>
            <Button Command="EditingCommands.ToggleBullets" CommandTarget="{Binding Element}" ToolTip="Bulleted List">
                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8FD;" FontSize="16"/>
            </Button>
            <Button Command="EditingCommands.ToggleNumbering" CommandTarget="{Binding Element}" ToolTip="Numbered List">
                <Image Source="/Icons/NumberedList.png" Width="18" Height="18" />
            </Button>
            <Separator/>
            <Button Click="InsertImage_Click" ToolTip="Insert Image">
                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8B9;" FontSize="16"/>
            </Button>
        </ToolBar>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            <Grid Background="Transparent" MouseLeftButtonDown="Workspace_MouseLeftButtonDown">
                <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="#FFEFF1F4" MouseLeftButtonDown="Workspace_MouseLeftButtonDown">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="20"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <local:Ruler x:Name="RulerH" Grid.Row="0" Grid.Column="1" Height="20" Orientation="Horizontal" Units="{Binding Units}"/>
                        <local:Ruler x:Name="RulerV" Grid.Row="1" Grid.Column="0" Width="20" Orientation="Vertical" Units="{Binding Units}"/>
                        <Border Grid.Row="1" Grid.Column="1" Width="{Binding CardWidth}" Height="{Binding CardHeight}" CornerRadius="24" BorderBrush="#FFCCD1D8" BorderThickness="2" Background="{StaticResource CheckerBrush}">
                            <Canvas x:Name="CardCanvas" Width="{Binding CardWidth}" Height="{Binding CardHeight}" Background="Transparent" Focusable="True">
                                <Line x:Name="GuideH" Stroke="#660078D7" StrokeThickness="1.5" Visibility="Collapsed"/>
                                <Line x:Name="GuideV" Stroke="#660078D7" StrokeThickness="1.5" Visibility="Collapsed"/>
                                <Rectangle x:Name="Marquee" Stroke="#770078D7" StrokeDashArray="3,2" Fill="#220078D7" Visibility="Collapsed"/>
                            </Canvas>
                        </Border>
                    </Grid>
                </ScrollViewer>
                <TextBlock x:Name="MarkerReadout" Margin="30,0,0,4" HorizontalAlignment="Left" VerticalAlignment="Bottom" Background="#CCFFFFFF" IsHitTestVisible="False" Foreground="Black"/>
            </Grid>
            <Border Grid.Column="1" Margin="12,0,0,0" Padding="12" Background="White" BorderBrush="#FFCCD1D8" BorderThickness="1" CornerRadius="12">
                <TabControl>
                    <TabItem Header="Inspector">
                        <StackPanel>
                            <ItemsControl ItemsSource="{Binding SelectedItems}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Tag}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <StackPanel DataContext="{Binding Inspector}" Margin="0,12,0,0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <!--0-->
                                        <RowDefinition Height="Auto"/>
                                        <!--1-->
                                        <RowDefinition Height="Auto"/>
                                        <!--2-->
                                        <RowDefinition Height="Auto"/>
                                        <!--3-->
                                        <RowDefinition Height="Auto"/>
                                        <!--4-->
                                        <RowDefinition Height="Auto"/>
                                        <!--5-->
                                        <RowDefinition Height="Auto"/>
                                        <!--6-->
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Name:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" TextAlignment="Right"/>
                                    <TextBox Text="{Binding ControlName, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1"/>
                                    <TextBlock Text="X:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" TextAlignment="Right" Visibility="{Binding CanEditPosition, Converter={StaticResource BoolToVis}}"/>
                                    <TextBox Text="{Binding X, UpdateSourceTrigger=PropertyChanged, StringFormat=F0}" Grid.Row="1" Grid.Column="1" Visibility="{Binding CanEditPosition, Converter={StaticResource BoolToVis}}"/>
                                    <TextBlock Text="Y:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" TextAlignment="Right" Visibility="{Binding CanEditPosition, Converter={StaticResource BoolToVis}}"/>
                                    <TextBox Text="{Binding Y, UpdateSourceTrigger=PropertyChanged, StringFormat=F0}" Grid.Row="2" Grid.Column="1" Visibility="{Binding CanEditPosition, Converter={StaticResource BoolToVis}}"/>
                                    <TextBlock Text="Width:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" TextAlignment="Right"/>
                                    <TextBox Text="{Binding WidthInput, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Grid.Column="1"/>
                                    <TextBlock Text="Height:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" TextAlignment="Right"/>
                                    <TextBox Text="{Binding HeightInput, UpdateSourceTrigger=PropertyChanged}" Grid.Row="4" Grid.Column="1"/>
                                    <TextBlock Text="Rotation:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" TextAlignment="Right"/>
                                    <TextBox Text="{Binding Rotation, UpdateSourceTrigger=PropertyChanged, StringFormat=F0}" Grid.Row="5" Grid.Column="1"/>
                                    <TextBlock Text="Hidden:" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" TextAlignment="Right"/>
                                    <CheckBox IsChecked="{Binding IsHidden, UpdateSourceTrigger=PropertyChanged}" Grid.Row="6" Grid.Column="1" IsEnabled="{Binding Element, Converter={StaticResource NullToBoolInverse}}"/>
                                </Grid>
                                
                                <Grid Visibility="{Binding IsImage, Converter={StaticResource BoolToVis}}" Margin="0,10,0,0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="File:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" TextAlignment="Right"/>
                                    <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="1">
                                        <TextBox Width="130" Text="{Binding ImageSourcePath, UpdateSourceTrigger=PropertyChanged}" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                                        <Button Content="Browse…" Click="BrowseImage_Click" Margin="6,0,0,0"/>
                                    </StackPanel>
                                    <TextBlock Text="Stretch:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" TextAlignment="Right"/>
                                    <ComboBox Grid.Row="1" Grid.Column="1" Width="130" HorizontalAlignment="Left" ItemsSource="{Binding StretchModes}" SelectedItem="{Binding ImageStretch, UpdateSourceTrigger=PropertyChanged}"/>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </TabItem>
                    <TabItem Header="Cards">
                        <StackPanel>
                            <ListView ItemsSource="{Binding Cards}" SelectedItem="{Binding SelectedCard}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBox Width="100" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                                            <TextBox Width="40" Margin="6,0,0,0" Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <Button Content="Add" Command="{Binding AddCardCommand}" Width="60"/>
                                <Button Content="Remove" Command="{Binding RemoveCardCommand}" Width="60" Margin="8,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </TabItem>
                </TabControl>
            </Border>
        </Grid>
    </DockPanel>
</Window>
